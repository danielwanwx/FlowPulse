# The default local profile contains no generated observations. Real samples
# arrive from the pinned Astronomy Shop Collector spool. Deterministic provider
# payloads live in unit tests only and are never loaded by this runtime.
groups: []
